import 'package:flutter/material.dart';

class OfflineGame extends StatelessWidget {
  const OfflineGame({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
